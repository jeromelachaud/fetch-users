The application fetches some fake user data from a remote JSON API.

When the "Load More" button is tapped, the application fetches more users and appends them to the page.

-------
To get started:

```
npm install
npm start
```
